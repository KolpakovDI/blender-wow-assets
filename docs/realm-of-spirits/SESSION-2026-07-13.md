# Session Notes — 2026-07-13

## Completed (день)

- Quest loop reliability:
  - `CatchDifferentSpirits` now tracks unique `CaughtIds` (no duplicate counting)
  - `LevelUpSpirit` progress supported
  - AcceptQuest seeds catch/level objectives from already owned spirits
  - Battle win grants XP to current spirit + quest progress
- Genkan slippers: pink fabric parts welded to feet in Safe/Genkan; removed on Exit/Combat
- Zone music / ZoneSystem / WorldLoot / QuestUI Focus / Mika 2D-Live / Gacha Robux path

## Completed (вечер) — Otaku Haven 2.0 + арена

- Магазин **×2** (пол 76×76), сдвиг `HavenCenter = (-25, 0, 28)` — **не пересекает Combat**
- Safe увеличен `(100, 28, 160)`; Combat `(105, 1, 45)` size 90
- **2 этаж:** RoomA / RoomB, лестница anime, балкон, сплошная черепичная крыша
- Стены 2 этажа **wall2H = 12**, крыша `roofLift = 2.5` (не бить головой у скатов)
- **Фусума** между комнатами (дракон / бамбук), E чтобы раздвинуть
- Мика снаружи `QuestMasterPosition = (-12, 0, -38)` (~28 studs от фасада), в Safe
- Spawn `(-25, 1, -45)`; BattleArena cyberpunk + `BattleArenaBuilder`
- Баланс early-game, gacha/bags sync, loot LOS — см. CHANGELOG

### Studio modules (источник истины — place `.rbxl`)

| Module | Path |
|--------|------|
| ZoneConfig | `ReplicatedStorage.RealmOfSpirits.ZoneConfig` |
| OtakuHavenBuilder | `ServerScriptService.RealmOfSpirits.OtakuHavenBuilder` |
| BattleArenaBuilder | `ServerScriptService.RealmOfSpirits.BattleArenaBuilder` |

Зеркала в git: `docs/realm-of-spirits/studio/*.lua` (после синка).

## Verify in Play

1. Genkan → slippers + GenkanBGM + bell
2. 2 этаж → лестница → фусума → балкон; крыша без дыр, голова не клипает
3. Мика снаружи южнее фасада, Safe
4. Shop не в Combat; выход в Akihabara / арена

## ⚠️ ЗАВТРА НАЧАТЬ С ЭТОГО (обязательно)

1. **Skills** — каталог способностей (`SkillCatalog` / данные в `SpiritDatabase`), эффекты, CD, MP
2. **Orchestrator** — оркестратор боя (порядок ходов / применение скиллов / синк UI)
3. Затем обвязка: `BattleSystem` / `GameManager` / боевой UI под новые скиллы

Не начинать сессию с декора магазина — сначала боевой пайплайн скиллов.

## Next (после скиллов)

- Developer Product → `ZoneConfig.GachaRobuxProductId`
- PvP / больше духов
- Ctrl+S place перед закрытием Studio
