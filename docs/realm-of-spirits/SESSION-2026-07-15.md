# Session Notes — 2026-07-15

## Итог дня

| Трека | Статус |
|-------|--------|
| P1 Identity | **PASS** (auto Edit + manual Play) |
| P1 Explore A | **PASS** |
| P1 Explore B | **PASS** |
| P1 Explore C | код + UX списка **готово**; полный funnel 101 в Play — **завтра** |

## P1 Identity — что сделано

- Карточка духа: навыки из каталога, прогресс evo, кнопка по требованиям
- Evolve: `SkillIds`, `OldName`, баннер `EvolutionSuccess`, `BonusHP` в бою
- Rank: `PromoteRank` → `CopperCoins`; Профиль «Ранг →» ≤2 клика
- ZoneController: EntranceBell (Model → BasePart Touched); баннер Haven справа
- DevBoost (Studio): `[DEV] Evo Boost` + LeftAlt+B (не F9)
- Фикс: `GameManager` DevBoost/`end)` ломал загрузку → пустые слоты духов

## P1 Explore — что сделано

### Slice A — PASS
- Tutorial FireCrystal на Exit→Combat; лут внутри Combat AABB
- Toast Exit/Combat про кристаллы; имена лута из `ItemCatalog`

### Slice B — PASS
- Ice crystal ItemId **102**; loot: fire / ice / chest (`fire=7 ice=3 chests=4`)

### Slice C — код + UX
- Side **103**: UniqueItem 7 «Посох Хранителя» + 3 зелья (не ghost Inventory Id7)
- Side **105**: `Prerequisites = {101,102,103,104}`
- Баг UX: «Помощь торговцу» (101) **был в Available**, но прятался ниже короткого списка (`pairs` без сортировки)
  - Fix: `BuildAvailableQuests` → sort Level→Id; `QUEST_LIST_H = 120`
  - Порядок на старте: Первые шаги → **Помощь торговцу** → 102 → 103

## Studio modules (трогали сегодня)

| Module | Path |
|--------|------|
| QuestSystem | `ServerScriptService.RealmOfSpirits.QuestSystem` |
| QuestUI | `StarterPlayer.StarterPlayerScripts.QuestUI` |
| WorldLootService | `ServerScriptService.RealmOfSpirits.WorldLootService` |
| GameManager | `ServerScriptService.RealmOfSpirits.GameManager` |
| ZoneController | `StarterPlayer.StarterPlayerScripts.ZoneController` |
| UIController | `StarterGui.UIController` |
| ClientController | `StarterPlayer.StarterPlayerScripts.ClientController` |

Place SoT: `C:\Mimo\RealmOfSpirits\RealmOfSpirits second.rbxl` — **Ctrl+S**

Зеркала: `docs/realm-of-spirits/studio/` (QuestSystem / QuestUI sort+list height синкнуты).

## Не закрыто сегодня

- Manual Play funnel Explore C: принять **101** → 5× огненный кристалл → сдать у Мики
- Полный проход side 102–105 не обязателен завтра в один заход; достаточно PASS по 101 + смоук наград 103 в коде

## Завтра — см. NEXT-SESSION.md
